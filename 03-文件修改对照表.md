# 文件修改对照表

这份表是给你以后在 VS 里改字、改图、改按钮时直接查的。

## 一、先分清 3 套内容

### 1. 旧静态页

目录：

`F:\择日择吉项目\上线版\网站程序\frontend`

特点：

- 改完保存，刷新通常就能看到
- 不需要构建
- 大量 `.html` 页面都在这里

### 2. React 页面

目录：

`F:\择日择吉项目\上线版\网站程序\app-frontend\src`

特点：

- 改完保存还不够
- 必须重新运行 `重新构建React前端.bat`
- 构建后页面才会同步到线上成品

### 3. 内容配置和图片

目录：

`F:\择日择吉项目\上线版\网站程序\content-hub`

特点：

- 主要管首页主视觉、二维码、老师图、案例图、联系信息
- 很多内容不是写死在 React 里，而是从这里读

## 二、页面和文件对应关系

### 首页 `http://localhost:8787/`

主要入口：

- `app-frontend\src\App.jsx`
- `app-frontend\src\components\HeroSection.jsx`
- `app-frontend\src\components\WhySection.jsx`
- `app-frontend\src\components\DecisionSection.jsx`
- `app-frontend\src\components\ServiceSection.jsx`
- `app-frontend\src\components\FeatureEntrySection.jsx`
- `app-frontend\src\components\KnowledgeSection.jsx`
- `app-frontend\src\components\CasesSection.jsx`
- `app-frontend\src\components\LeadCaptureSection.jsx`

首页主视觉文案和图片，更常用的是这里：

- `content-hub\text-settings\homepage-hero.json`

这里可以改：

- 主标题
- 副标题
- 细节文案
- 按钮文字
- 首页主图地址
- 图片模式

### 首页联系信息 / 微信号 / 页脚联系文案

改这里：

- `content-hub\text-settings\contact.json`

这里可以改：

- 页脚联系文案
- 微信号
- 手机号
- 二维码提示语
- 联系备注

### 老师介绍相关文案 / 老师名称 / 服务说明

改这里：

- `content-hub\text-settings\teacher-profile.json`

老师图片文件放这里：

- `content-hub\teacher-images`

当前启用哪一张图，绑定关系在这里：

- `content-hub\text-settings\active-assets.json`

### 案例页 `http://localhost:8787/cases.html`

React 源码：

- `app-frontend\src\cases-main.jsx`
- `app-frontend\src\pages\CasesGalleryPage.jsx`

案例图文件：

- `content-hub\case-images`

### 年度顾问页 `http://localhost:8787/annual.html`

React 源码：

- `app-frontend\src\annual-main.jsx`
- `app-frontend\src\pages\AnnualConsultingPage.jsx`

### 顾问页 `http://localhost:8787/advisor.html`

React 源码：

- `app-frontend\src\advisor-main.jsx`
- `app-frontend\src\pages\AdvisorPage.jsx`

### 合作页 `http://localhost:8787/partner.html`

React 源码：

- `app-frontend\src\partner-main.jsx`
- `app-frontend\src\pages\PlaceholderLandingPage.jsx`

合作页现在是通过入口文件把配置传给通用页面，不是单独的 `PartnerPage.jsx`。

如果以后你找不到对应文件，就在：

- `app-frontend\src`

里搜索页面标题文字即可。

### 资料介绍页 `http://localhost:8787/profile.html`

React 源码：

- `app-frontend\src\profile-main.jsx`
- `app-frontend\src\pages\PlaceholderLandingPage.jsx`

### 表单页 `http://localhost:8787/guide-form`

这页不是 React 页。

改这里：

- `frontend\index.html`

表单行为和复制微信、提交逻辑，通常看这里：

- `frontend\script.js`
- `frontend\config.js`

### 后台页 `http://localhost:8787/admin.html`

改这里：

- `frontend\admin.html`

### 内部工具总入口 `http://localhost:8787/internal-tools.html`

改这里：

- `frontend\internal-tools.html`

这个页面是给你自己和开发人员用的目录页，不是给客户看的。

### 城市页 / SEO 页

都在这里：

- `frontend\*.html`

例如：

- `frontend\zhuhai-factory-start.html`
- `frontend\dongguan-company-move.html`
- `frontend\boss-absent-start.html`

它们基本是谁的页面，就改谁同名的 `.html`。

## 三、改字时最常见的判断方法

### 情况 1：你改了文件，刷新马上生效

说明你改的是 `frontend` 或 `content-hub`。

### 情况 2：你改了文件，刷新没变化

大概率是你改了 `app-frontend\src`。

这时要再执行一次：

`F:\择日择吉项目\上线版\网站程序\重新构建React前端.bat`

## 四、最常改的 6 个位置

1. 首页第一屏文案

优先看：

- `content-hub\text-settings\homepage-hero.json`
- `app-frontend\src\components\HeroSection.jsx`

2. 首页“为什么企业需要专业择吉”

看：

- `app-frontend\src\components\WhySection.jsx`

3. 案例页标题、卡片、按钮

看：

- `app-frontend\src\pages\CasesGalleryPage.jsx`

4. 年度顾问页文案

看：

- `app-frontend\src\pages\AnnualConsultingPage.jsx`

5. 表单页内容

看：

- `frontend\index.html`

6. 二维码、微信号、联系方式

看：

- `content-hub\text-settings\contact.json`
- `content-hub\qr-images`
- `content-hub\text-settings\active-assets.json`

## 五、你以后最稳的操作顺序

1. 先确定这个页面是 React 页还是静态页
2. 在 `上线版\网站程序` 里改，不要再改旧目录
3. 如果改的是 `frontend`，直接刷新
4. 如果改的是 `app-frontend\src`，先运行 `重新构建React前端.bat`
5. 再刷新 `http://localhost:8787`
