# 部署说明

## 一、推荐你先用本机启动检查

进入：

`F:\择日择吉项目\上线版\网站程序`

然后双击：

`启动网站.bat`

如果你只是想关闭本机正在运行的网站，可以双击：

`停止网站.bat`

启动成功后，常用地址是：

- 首页：`http://localhost:8787`
- 表单页：`http://localhost:8787/guide-form`
- 案例页：`http://localhost:8787/cases.html`
- 年度顾问页：`http://localhost:8787/annual.html`
- 后台：`http://localhost:8787/admin.html`
- 内部工具入口：`http://localhost:8787/internal-tools.html`

## 二、正式上线前必须配置

打开：

`F:\择日择吉项目\上线版\网站程序\backend`

把：

`.env.example`

复制一份，命名为：

`.env`

然后至少改这几项：

```env
PORT=8787
ADMIN_TOKEN=这里换成你自己的长密钥
WEBHOOK_URL=这里填飞书机器人Webhook
WEBHOOK_TYPE=feishu
ALLOW_OPEN_ADMIN_WHEN_NO_TOKEN=false
RATE_WINDOW_MS=600000
RATE_MAX_LEADS=5
```

## 三、这套网站实际怎么运行

这套站不是只传一个网页文件夹就行，它是一起运行的：

1. `backend`
   负责表单接口、后台接口、飞书提醒、静态资源分发
2. `frontend`
   负责旧静态页，比如 `/guide-form`、大量 SEO 页、后台页
3. `app-frontend/dist`
   负责 React 页面成品，比如首页、案例页、年度顾问页
4. `content-hub`
   负责当前启用的图片、二维码、首页主图、联系信息等内容

## 四、如果你要把它部署到服务器

最稳的方式有 2 种。

### 方式 A：普通 Node 服务器

1. 服务器安装 Node.js 20
2. 把 `网站程序` 整个目录上传到服务器
3. 在服务器里进入 `网站程序\backend`
4. 执行：

```powershell
npm install
npm start
```

5. 确保服务器放行 8787 端口，或者再用 Nginx 反向代理到域名

### 方式 B：Docker

我已经把 `网站程序\Dockerfile` 改成了可直接构建 React 前端的版本。

在 `网站程序` 目录执行：

```powershell
docker build -t kaigong-site .
docker run -d -p 8787:8787 --name kaigong-site kaigong-site
```

如果要带环境变量，可以这样：

```powershell
docker run -d -p 8787:8787 --name kaigong-site --env-file backend/.env kaigong-site
```

## 五、如果你部署到 Render

可参考：

`F:\择日择吉项目\上线版\网站程序\deploy\render.yaml`

我已经改成：

- 先安装 `backend` 依赖
- 再安装 `app-frontend` 依赖
- 自动构建 React `dist`
- 最后启动 `backend`

Render 上要手动填写的环境变量：

- `ADMIN_TOKEN`
- `WEBHOOK_URL`
- `WEBHOOK_TYPE`
- `ALLOW_OPEN_ADMIN_WHEN_NO_TOKEN=false`

## 六、上线后数据存在哪里

线索会保存在：

- `网站程序\backend\data\leads.json`
- `网站程序\backend\data\leads.csv`

建议你定期备份 `backend\data`。

## 七、上线后如果页面没变化，先查这 4 件事

1. 你是不是改错目录了。以后只改 `上线版\网站程序` 里的文件。
2. 你改的是不是 React 源码。如果是，就要重新运行 `重新构建React前端.bat`。
3. 你刷新的是不是正确网址。不要再直接双击 `index.html`。
4. 你是不是还开着旧目录的服务。以后优先用 `上线版\网站程序\启动网站.bat` 启动。

## 八、关于内部工具页

- `internal-tools.html` 是我新增的内部入口页，方便你和开发人员集中进入后台、内容中心、运营工具页。
- 它不是给客户看的，不要从首页、案例页、表单页等公开页面加入口。
- 现阶段我没有搬动这些真实文件位置，只是先把入口和说明分清，确保现有网址不受影响。
