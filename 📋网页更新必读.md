# 📋 网页更新必读（每次动网页前必须阅读）

> **重要提示**：每次修改上线版网站的任何文件之前，必须先阅读本文件，避免重复发生以下事故。
> 
> **最后更新**：2026-09-08
> **维护者**：知远风物志团队

---

## 🚨 事故清单（历史错误，不得再犯）

### 事故1：正则表达式匹配错误，覆盖原有文章
- **发生时间**：2026-09-08
- **错误描述**：用Python脚本批量更新index.html时，正则表达式`(.*?)(</div>\s*</div>)`的非贪婪匹配错误，把原有16篇文章卡片全部覆盖，只保留了新增的13篇GEO-06文章
- **根本原因**：没有仔细分析HTML结构，`</div></div>`可能匹配到article-grid内部的div闭合，而不是article-grid本身的闭合
- **正确做法**：
  1. 先仔细读取HTML文件，分析实际结构
  2. 用"找到最后一篇文章的`</a>`后插入"的方式，而不是用div闭合匹配
  3. 执行脚本前先备份文件（`git checkout`或复制文件）
  4. 执行后立即验证文章数量是否正确
- **恢复方式**：`git checkout HEAD~1 -- 网站程序/frontend/min-su-yan-jiu/index.html`

### 事故2：文章卡片HTML结构判断错误
- **发生时间**：2026-09-08
- **错误描述**：假设文章卡片是`<a class="article-card" href="...">`，实际是`<a href="..." class="article-card">`（class在href后面），导致正则`<a class="article-card"`匹配不到任何文章
- **根本原因**：没有先读取HTML文件确认实际结构，凭印象写正则
- **正确做法**：
  1. 写任何正则之前，先读取HTML文件确认实际标签顺序和属性
  2. 用更灵活的正则：`<a[^>]*class="article-card"[^>]*>`（不管class和href的顺序）
  3. 先在小范围内测试正则是否匹配，再批量执行

### 事故3：分类映射错误，GEO-06被归为"旧文改造"
- **发生时间**：2026-09-08
- **错误描述**：article-manager.js的CATEGORY_MAP只定义了01-04，GEO-06的文章被inferCategoryFromFilename函数归为默认的"传统文化课堂"，后来又被手动归为"旧文改造"，没有按内容分配到四个分类
- **根本原因**：没有通读GEO-06文章的内容，凭编号分类，而不是按内容分类
- **正确做法**：
  1. 新增文章分类时，必须通读文章内容，按主题分配到现有分类
  2. 不要轻易新增分类，保持分类体系稳定（目前只有4个分类：择吉习俗、岁时信仰、族群地域、社会历史）
  3. 如果文章内容跨分类，选择最主要的主题分类
  4. 更新articles.json时，必须手动指定每篇文章的分类，不能依赖自动推断

### 事故4：统计数字没有同步更新
- **发生时间**：2026-09-08
- **错误描述**：新增13篇文章后，index.html顶部的"16篇深度研究"和底部内容地图的"全站16篇深度研究"都没有更新，仍然显示16篇
- **根本原因**：只关注了文章卡片的添加，忽略了页面中其他引用文章数量的地方
- **正确做法**：
  1. 新增/删除文章后，必须搜索整个index.html中所有包含文章数量的地方
  2. 至少检查：顶部统计数字、内容地图说明、页面描述、meta description
  3. 用全局搜索"16篇"、"篇深度研究"等关键词，确保全部更新

### 事故5：封面图为空，用渐变背景代替
- **发生时间**：2026-09-08
- **错误描述**：新增13篇GEO-06文章时，有5篇文章没有对应的封面图，直接用了CSS渐变背景（`linear-gradient(...)`），导致首页卡片显示为纯色块，和其他有图的卡片不一致，视觉效果差
- **根本原因**：没有提前准备封面图，为了省事直接用渐变背景
- **正确做法**：
  1. 新增文章前，必须先准备好封面图（古风中国风，暖棕色调，和现有封面图风格一致）
  2. 封面图尺寸建议：1200×600或类似比例
  3. 封面图放在`min-su-yan-jiu/images/`目录下，命名规范：`hero-关键词.jpg`
  4. 如果暂时没有封面图，用现有的相关封面图暂代，不要用渐变背景

### 事故6：后端API返回0篇文章
- **发生时间**：2026-09-08
- **错误描述**：article-manager.js的listArticles()从data/articles.json读取，但这个文件不存在，导致/api/articles/list返回0篇文章，后台文章管理页面显示空列表
- **根本原因**：articles.json是手动维护的，但没有创建，也没有在listArticles()中做fallback处理
- **正确做法**：
  1. 确保data/articles.json存在且包含所有文章
  2. listArticles()必须有fallback：如果articles.json不存在或为空，自动扫描HTML目录生成
  3. 新增文章后，必须更新articles.json（或删除它让它自动重新生成）
  4. 修改后端代码后，必须重启后端服务并验证API返回

---

## ✅ 网页更新标准流程（每次必须遵守）

### 步骤1：更新前准备
- [ ] 阅读本事故清单
- [ ] 确认要修改的文件路径和内容
- [ ] 用git status确认当前工作区状态
- [ ] 如果有未提交的修改，先提交或stash

### 步骤2：备份
- [ ] 对要修改的文件做备份（`git checkout`或复制文件）
- [ ] 如果是批量修改，先在小范围内测试，确认无误后再批量执行

### 步骤3：执行修改
- [ ] 仔细读取要修改的文件，确认实际结构（不要凭印象）
- [ ] 写脚本时，先打印匹配结果，确认匹配正确后再执行替换
- [ ] 执行后立即验证结果

### 步骤4：验证清单
- [ ] **文章数量**：index.html中的文章卡片数量是否正确
- [ ] **分类统计**：每个分类的文章数量是否正确
- [ ] **统计数字**：顶部统计数字、内容地图说明是否更新
- [ ] **封面图**：所有文章卡片是否都有封面图（无渐变背景）
- [ ] **HTML结构**：div标签是否平衡（开标签数=闭标签数）
- [ ] **页面访问**：http://localhost:8787/min-su-yan-jiu/ 返回200
- [ ] **后端API**：http://localhost:8787/api/articles/list 返回正确数量
- [ ] **后台页面**：http://localhost:8787/article-admin.html 显示正确文章列表
- [ ] **sitemap**：http://localhost:8787/sitemap.xml 包含所有文章

### 步骤5：提交
- [ ] git add .
- [ ] git commit -m "清晰的提交信息，说明修改了什么"
- [ ] 验证git log显示最新提交

---

## 📁 关键文件路径

| 文件 | 路径 | 说明 |
|---|---|---|
| 风物志首页 | `网站程序/frontend/min-su-yan-jiu/index.html` | 硬编码文章列表，新增文章需手动更新 |
| 文章目录 | `网站程序/frontend/min-su-yan-jiu/GEO-*.html` | 29篇文章详情页 |
| 封面图目录 | `网站程序/frontend/min-su-yan-jiu/images/` | 所有封面图 |
| 后端服务 | `网站程序/backend/server.js` | 后端主服务，8787端口 |
| 文章管理 | `网站程序/backend/article-manager.js` | 文章管理模块，包含分类映射 |
| 文章索引 | `网站程序/backend/data/articles.json` | 文章元数据，API从这里读取 |
| 后台页面 | `网站程序/frontend/article-admin.html` | 文章管理后台 |
| 规范和QA | `E:\仓库\文章创作\规范和QA\` | 所有规范文件和QA脚本 |

---

## 🔧 常用命令

```bash
# 重启后端服务
taskkill /F /IM node.exe
Start-Process -FilePath "node" -ArgumentList "`"F:\择日择吉项目\上线版\网站程序\backend\server.js`"" -WindowStyle Hidden

# 验证页面
curl.exe -s -o NUL -w "%{http_code}" "http://localhost:8787/min-su-yan-jiu/"

# 验证API
curl.exe -s "http://localhost:8787/api/articles/list" | ConvertFrom-Json

# 验证sitemap
curl.exe -s "http://localhost:8787/sitemap.xml"

# 从git恢复文件
git checkout HEAD~1 -- "网站程序/frontend/min-su-yan-jiu/index.html"

# 统计index.html中的文章卡片数量
(content = Get-Content "index.html" -Raw) -split 'class="article-card"' | Measure-Object | Select-Object -ExpandProperty Count
```

---

## ⚠️ 禁止事项

1. ❌ 不要凭印象写正则，必须先读取文件确认实际结构
2. ❌ 不要用渐变背景代替封面图
3. ❌ 不要轻易新增分类，保持4个分类体系稳定
4. ❌ 不要只更新文章卡片，忽略统计数字和内容地图
5. ❌ 不要修改后端后不重启服务
6. ❌ 不要不验证就提交git
7. ❌ 不要在没有备份的情况下批量修改文件

---

**本文件必须在每次修改上线版网站之前阅读。如果发生新的事故，必须及时补充到本文件中。**
